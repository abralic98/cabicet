query allUsers {
  allUsers {
    username
    id
  }
}

mutation createMessage($message: String!, $userId: ID!) {
  createMessage(message: $message, userId: $userId) {
    message
  }
}

query getMessages {
  getMessages {
    id
    message
    user {
      username
    }
  }
}
